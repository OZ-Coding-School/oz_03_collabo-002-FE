import Banner from '../common/Banner';
import Pagination from '../common/Pagination';

const Home = () => {
  return (
    <div>
      <Pagination />
      <Banner />
    </div>
  );
};

export default Home;
